# Agent Dashboard Environment Configuration

# === Frontend Configuration ===

# Set this to use an external/remote API server
# Leave empty for local development (relative URLs)
# Examples:
#   http://localhost:3001         (local API server)
#   https://api.example.com       (remote API)
#   https://xyz.trycloudflare.com (Cloudflare tunnel)
NEXT_PUBLIC_API_URL=

# === Supabase Configuration ===

# Supabase URL and Anonymous Key (safe to be public)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Key (for server-side use only, keep secret!)
# Use this in API server and sync service for admin operations
SUPABASE_SERVICE_KEY=your-service-key-here

# === API Server Configuration ===

# API server port (default: 3001)
# PORT=3001

# Home directory for session files and workspace
# (defaults to /home/vtto, usually not needed)
# HOME=/home/vtto
